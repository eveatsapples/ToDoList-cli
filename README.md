handy little todo list for use in the command line, you will need to build it in order to use it for now until I add it to some CDNs,
It's written in dotnetcore so it is usable on all operating systems.
